#include <stdlib.h>
#include <time.h>
#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (correct)
 */
int main(void)
{
	int i;																																		for (i = 0; i < 10; i++)
		printf("%i", i);
	printf("\n");
	return (0);
}
